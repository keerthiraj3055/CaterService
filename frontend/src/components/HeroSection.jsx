import React from 'react';

const HeroSection = () => (
  <section style={{ padding: '2rem', background: '#e9ecef', textAlign: 'center' }}>
    <h1>Welcome to FoodServe Catering</h1>
    <p>Delicious food, exceptional service, memorable events.</p>
  </section>
);

export default HeroSection;
